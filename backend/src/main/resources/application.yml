spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/assistwalk}
    username: ${DB_USERNAME:assistwalk_user}
    password: ${DB_PASSWORD:changeme}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate          # Flyway gère le schéma, pas Hibernate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8081

jwt:
  secret: ${JWT_SECRET:dev-secret-insecure-change-in-prod}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}

logging:
  level:
    com.assistwalk: DEBUG